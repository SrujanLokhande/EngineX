#include "Bullet.h"

Bullet::Bullet(exEngineInterface* pEngine, const exVector2& position, const exVector2& target) 
{
    //// Set the bullet's position to the initial position
    //Transform* transform = new Transform(position);
    //AddComponent(transform);

    //// Calculate the direction and speed of the bullet based on the target's position
    //exVector2 targetPosition = pTargetObject->FindComponentOfType<Transform>()->GetPosition();
    //exVector2 direction = (targetPosition - position).Normalized();
    //float speed = 500.0f;  // Adjust the speed as needed

    //// Add the PhysicsComponent to control the bullet's movement
    //PhysicsComponent* physicsComponent = new PhysicsComponent(direction, speed);
    //AddComponent(physicsComponent);
}
